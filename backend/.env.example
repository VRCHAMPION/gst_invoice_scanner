# ── GST Invoice Scanner — Environment Variables ──────────────────────────────
# Copy this file to .env and fill in your actual values.
# NEVER commit the .env file to version control.

# Groq API Key (get from https://console.groq.com/keys)
GROQ_API_KEY=your_groq_api_key_here

# Supabase PostgreSQL connection string (get from Supabase Dashboard > Settings > Database)
# Use the "Transaction Mode" pooled URL (port 6543) with your password URL-encoded
# e.g. if password has @, replace it with %40
DATABASE_URL=postgresql://postgres.your_project_id:your_password@aws-0-region.pooler.supabase.com:6543/postgres?sslmode=require

# JWT Secret — use a long random string (at least 32 chars)
JWT_SECRET=change-me-to-a-long-random-secret-string
