{
  "name": "gst-invoice-scanner",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node -e \"const fs=require('fs');const f='frontend/js/config.js';fs.writeFileSync(f,fs.readFileSync(f,'utf8').replace('REPLACE_WITH_SUPABASE_ANON_KEY',process.env.SUPABASE_ANON_KEY))\""
  }
}
